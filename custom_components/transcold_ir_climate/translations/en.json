{
  "config": {
    "step": {
      "user": {
        "title": "Set up Transcold IR Climate",
        "description": "Select your IR remote entity (e.g. Broadlink) OR enter an ESPHome service that transmits raw IR timings.",
        "data": {
          "name": "Name",
          "remote_entity": "IR Remote Entity (Broadlink etc.)",
          "esphome_service": "ESPHome service (e.g. esphome.ir_proxy_send_raw_command)",
          "min_temp": "Minimum Temperature",
          "max_temp": "Maximum Temperature",
          "target_temp": "Target Temperature",
          "initial_operation_mode": "Initial Operation Mode",
          "command_format": "Command Format (auto-detected for Broadlink)",
          "protocol": "IR Protocol"
        }
      }
    },
    "error": {
      "need_target": "Select an IR remote entity or enter an ESPHome service."
    },
    "abort": {
      "already_configured": "This IR remote is already configured.",
      "no_protocols": "No IR protocols found."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Transcold IR Climate Options",
        "description": "Adjust the AC settings.",
        "data": {
          "min_temp": "Minimum Temperature",
          "max_temp": "Maximum Temperature",
          "target_temp": "Target Temperature",
          "initial_operation_mode": "Initial Operation Mode",
          "command_format": "Command Format (auto-detected for Broadlink)"
        }
      }
    }
  },
  "selector": {
    "protocol": {
      "options": {
        "transcold": "Transcold"
      }
    }
  }
}
